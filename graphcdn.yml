name: brianlovin
originUrl: 'https://brianlovin.com/api/graphql'
rules:
  - description: Cache authentication for a month
    maxAge: 2592000
    scope: ADMIN
    swr: 15
    types:
      Query:
        isMe: true
  - description: Cache single post hourly
    maxAge: 3600
    scope: PUBLIC
    swr: 30
    types:
      Query:
        post: true
  - description: Cache bookmarks and AMAs for 15 minutes
    maxAge: 900
    scope: PUBLIC
    swr: 900
    types:
      Query:
        amaQuestions: true
        bookmarks: true
  - description: Cache repos weekly
    maxAge: 604800
    scope: PUBLIC
    swr: 30
    types:
      Query:
        repos: true
  - description: Cache episodes daily
    maxAge: 86400
    scope: PUBLIC
    swr: 30
    types:
      Query:
        episodes: true
  - description: Cache posts daily
    maxAge: 86400
    scope: PUBLIC
    swr: 30
    types:
      Query:
        posts: true
schema: 'http://localhost:3000/api/graphql'
scopes:
  ADMIN: 'cookie:session'
